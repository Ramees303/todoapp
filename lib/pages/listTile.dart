import 'package:flutter/material.dart';

class MyListViewSeperator extends StatelessWidget {
  const MyListViewSeperator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}